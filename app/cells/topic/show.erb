<section class="topic">
  <h1><%= @topic.title %></h1>

  <%= render_cell_collection :post, :show, @topic.posts.includes(:images) %>

  <div class="actions">
    <%= link_to 'Reply', @topic %>
  </div>
</section>
